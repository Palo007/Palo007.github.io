# This repo is PUBLIC (GitHub Pages user site). Never commit secrets.

# === Secrets: signing key and its password ===
/android.keystore
/keystore.pass
*.keystore
*.jks
local.properties

# === Build outputs and APKs ===
/.gradle
/build
/app/build/
/builds
*.apk
*.idsig
*.aab

# === Local tooling, logs and scratch ===
/manifest-checksum.txt
/store_icon.png
/user-site
/_build-driver.js
/_init-driver.js
/_init-driver.log
/_init-raw.log
/_render-icons.js
*.log
/.omo/
/.cline/
/.kilo/
/.idea/

# === Notes: only README.md is tracked ===
*.md
!/README.md

# Tracked on purpose (were ignored before 2026-09-24): the Android source in
# /app (hand-owned native code, see README), the root Gradle files, the Gradle
# wrapper and twa-manifest.json, so the app can be rebuilt from a clone.
